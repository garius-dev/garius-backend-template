apiVersion: v1
kind: Service
metadata:
  name: {{ include "garius.fullname" . }}
  labels:
    {{- include "garius.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "garius.selectorLabels" . | nindent 4 }}
