{
  "_comment": [
    "Produção NÃO define segredos aqui — eles vêm do Google Secret Manager (fonte padrão)",
    "ou de variáveis de ambiente.",
    "",
    "Os defaults FECHADOS do appsettings.json valem aqui de propósito:",
    "  Cors.AllowedOrigins = []   -> nenhuma origem permitida (nega tudo)",
    "  A documentação (/scalar) exige a permissão docs.read — não há flag para esquecer ligada.",
    "  Health.ApiKey = ''         -> /health/detail NÃO é mapeado",
    "",
    "Uma falha de configuração deve virar uma falha FECHADA, nunca aberta."
  ],

  "Serilog": {
    "Loki": {
      "Enabled": true
    }
  },

  "_comment_producao": [
    "Este arquivo NÃO fixa endereço nenhum, e é de propósito: em produção eles mudam por",
    "SERVIDOR, e um valor hardcoded aqui seria a primeira coisa a esquecer de trocar.",
    "",
    "O que vem de FORA, no Secret Manager (junto com as senhas):",
    "",
    "    \"Database:Host\":             \"postgres-prod\"    <- nome do CONTAINER, não localhost",
    "    \"Redis:ConnectionString\":    \"redis-prod:6379\"      (dentro de um container,",
    "    \"Security:TrustedProxies:0\": \"172.18.0.0/16\"        localhost é ele mesmo)",
    "    \"Cors:AllowedOrigins:0\":     \"https://app.suaapp.com\"",
    "",
    "A aplicação NÃO SOBE sem eles — falha fechada, e o log diz qual falta.",
    "",
    "O TrustedProxies é a rede Docker do TRAEFIK (não a da Cloudflare, que é outra camada e já",
    "vem ligada em TrustCloudflareIps). Pegue o valor no servidor, uma vez:",
    "",
    "    docker network inspect garius_network --format '{{(index .IPAM.Config 0).Subnet}}'"
  ],

  "_comment_gcp": [
    "ProjectId e SecretName NÃO são segredo — o segredo é o CONTEÚDO do secret, e ele nunca",
    "encosta neste arquivo. São só coordenadas: onde procurar.",
    "",
    "⚠️ Eles PRECISAM estar aqui. Antes, `Enabled: true` vinha sozinho e as coordenadas só",
    "existiam no appsettings.Development.json — então toda app derivada MORRIA no primeiro",
    "boot em produção, com:",
    "",
    "    GcpSecrets:Enabled=true, mas ProjectId ou SecretName não foram configurados.",
    "",
    "O SecretName é derivado pelo `dotnet new` a partir do nome da app. O ProjectId é o do seu",
    "projeto no GCP — troque-o se a sua app viver em outro."
  ],

  "GcpSecrets": {
    "Enabled": true,
    "ProjectId": "garius-tcm",
    "SecretName": "gariustech-backend-template-secrets"
  }
}
